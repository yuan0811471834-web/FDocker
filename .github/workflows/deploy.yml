name: FDocker

on:
  push:
    branches: ["main"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./
          push: true
          tags: yuan5462/labs-nestjs-action:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    runs-on: self-hosted
    needs: build-and-push
    steps:
      - name: Deploy to Nginx UI
        run: |
          echo "🚀 Starting Docker Deployment..."

          # ใช้ทางลัดแบบระบุชื่อ User ตรงๆ เพื่อไม่ให้ Runner หลงทาง
          cd /home/yuan/app/nginx-ui

          # ตรวจสอบว่าไฟล์อยู่จริงไหมก่อนเริ่มรัน (เพื่อ debug)
          ls -la docker-compose.yml

          # รันคำสั่ง Docker (ไม่ต้องมี sudo)
          docker compose pull
          docker compose up -d --remove-orphans
          docker image prune -f

          echo "🎉 Docker Deployment completed!"