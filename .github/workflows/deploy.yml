name: FDocker

on:
  push:
    branches: ["main"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./
          push: true
          tags: yuan5462/labs-nestjs-action:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    runs-on: self-hosted
    needs: build-and-push
    steps:
      - name: Deploy to Production
        run: |
          echo "🚀 Starting Deployment..."
          
          # 1. เข้าไปยังโฟลเดอร์ที่มีไฟล์ docker-compose.yml จริงๆ
          cd $HOME/app/nginx-ui
          
          # 2. ดึง Image ล่าสุด (ไม่ต้องใส่ sudo แล้ว)
          docker compose pull
          
          # 3. สั่งรัน Container ใหม่
          docker compose up -d --remove-orphans
          
          # 4. ลบ Image เก่าๆ ที่ไม่ได้ใช้เพื่อประหยัดพื้นที่
          docker image prune -f
          
          echo "🎉 Deployment Successful!"